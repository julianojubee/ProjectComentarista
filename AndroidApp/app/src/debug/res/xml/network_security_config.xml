<?xml version="1.0" encoding="utf-8"?>
<!--
    Libera cleartext (HTTP) apenas para o alias do emulador ao host (10.0.2.2) e para
    localhost, só nesta variante de build. Testando num tablet físico na mesma rede,
    adicione o IP local da máquina dev aqui também (ex.: 192.168.0.10).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
